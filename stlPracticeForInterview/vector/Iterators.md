iterator is nothing but its object 
with the help of that object we can trevers and access the element of STL contaner.

const iterator: with the the help of constatnt iterator we can access the element but cant modify it